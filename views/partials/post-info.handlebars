<article class="post">
    <div class="title">
        <h2>{{title}} by {{user.username}} on {{format_date created_at}}</h2>
        <p>{{image}}</p>
        <p>{{description}}</p>
        <p>${{price}}</p>        
        <a href="/post/{{id}}">{{messages.length}} {{format_plural "message" messages.length}}</a>
    </div>
</article>